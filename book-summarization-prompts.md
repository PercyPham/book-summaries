Prompt to summarize book:

```txt
Summarize [INSERT_HERE]
Don't go chapter-by-chapter — instead pull out all the main points across the content, explain the reasoning behind each one, and show how the points connect, build on each other, or flow together, while cutting all preamble, filler, and anecdotal fluff. You can present this however makes sense (list, prose, mixed), as long as it covers: the main points with reasoning, how they connect, and the overall throughline.
```

Prompt to compose big picture example:

```txt
Using the summaries from the previous turns (all sections of the book combined), build a practical roadmap for applying the book's knowledge.

- If the book already uses a running example/case throughout, apply the roadmap to that same example. Otherwise, make it general-purpose so anyone trying to apply the book's ideas can follow it.
- Lay it out as sequential steps (the path from start to actually using this knowledge).
- For each step, list what needs to be checked/considered at that point — key decisions, risks, prerequisites, or signals that you're doing it right/wrong.
- Make sure the roadmap reflects how the points connect (from the summaries) — not just a checklist, but a logical progression that mirrors how the book's ideas build on each other.
- Skip filler — only actionable substance.
```
